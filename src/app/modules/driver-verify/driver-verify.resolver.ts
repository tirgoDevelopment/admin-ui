import { Routes } from '@angular/router';
import { DriverVerifyComponent } from './driver-verify.component';

export default [
    {
        path     : '',
        component: DriverVerifyComponent,
        resolve  : {
        },
    },
] as Routes;
