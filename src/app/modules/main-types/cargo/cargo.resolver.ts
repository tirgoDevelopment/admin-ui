import { Routes } from '@angular/router';
import { CargoComponent } from './cargo.component';

export default [
    {
        path: '',
        component: CargoComponent,
        resolve: {
        },
    },
] as Routes;
