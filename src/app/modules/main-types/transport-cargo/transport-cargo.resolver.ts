import { Routes } from '@angular/router';
import { TransportCargoComponent } from './transport-cargo.component';

export default [
    {
        path: '',
        component: TransportCargoComponent,
        resolve: {
        },
    },
] as Routes;
